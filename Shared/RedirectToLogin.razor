@inject NavigationManager Navigation

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Only redirect to login from the client side.
            // This prevents an immediate redirect during pre-rendering when JS (and thus session storage) is not available.
            Navigation.NavigateTo("/login");
        }
    }
}
